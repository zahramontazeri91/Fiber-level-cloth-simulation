** render a scene using mitsuba **

1. navigate to mitsuba/dist
2. mitsuba path/to/xml/file
ex: mitsuba ../scene/fibers.xml

in xml file you can change the yarn file 

** use python script to transfer file format (from obj to mitsuba yarn format) **
mitsuba/python/readOBJ